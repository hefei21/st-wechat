# 安全策略

## 支持范围

安全修复优先面向最新的 `1.0.x` 版本和当前默认分支。旧提交、第三方修改版以及未经验证的
SillyTavern 版本可能需要先在可复现环境中确认。

## 报告安全问题

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。不要在公开 Issue、
截图或日志中提交以下内容：

- iLink token、二维码内容、context token 或微信身份；
- LLM API key、Cookie、私钥或认证头；
- 真实聊天正文、角色私密数据、内部路径和公网入口；
- 可直接利用且尚未修复的完整攻击步骤。

报告中请提供受影响版本、最小复现步骤、预期与实际行为、脱敏诊断编号和影响判断。若仓库尚未
开启私密漏洞报告，可先创建不含敏感细节的公开 Issue，请维护者提供私密沟通方式。

## 部署责任边界

本插件继承 SillyTavern 的网络入口、认证、IP 白名单和 CSRF 防护。不要把未经保护的
SillyTavern 端口直接暴露到公网。`<dataRoot>/st-wechat/`、备份目录和模型密钥仅应授予必要的
宿主机或容器身份读取。

公开诊断前应再次检查脱敏结果；自动脱敏不能替代人工确认。
